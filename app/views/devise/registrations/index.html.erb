<div class=<%= mobile? ? "main_mobile" : "main" %>>
  <% @users.each do  |user| %>
    <div class="article">
      <p ><%= user.username %> </p>
      <p><%= user.email %> </p>
    </div>
  <% end %>
</div>